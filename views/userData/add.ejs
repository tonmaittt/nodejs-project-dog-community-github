<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เพิ่มผู้ใช้</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body class="container" style="margin-top: 50px;">

    <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>
    
    <a href="/userData" class="btn btn-info">กลับ</a>
    <br><br>

    <div class="card">
        <div class="card-header">
            เพิ่มผู้ใช้
        </div>
        <div class="card-body">
            <form action="/userData/add" method="post">
                <div class="form-group">
                    <label>ชื่อ:</label>
                    <input type="text" class="form-control" name="fname" value="<%= fname %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label>สกุล:</label>
                    <input type="text" class="form-control" name="sname" value="<%= sname %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-info" value="Add">
                </div>
            </form>
        </div>
    </div>

</body>
</html>